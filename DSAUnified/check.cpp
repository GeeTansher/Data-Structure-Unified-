#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int N=1e5+2;
    int n=5;
    vector<int> graph[n];
    // if(sizeof(graph)==0){
        // graph.clear();
        int c=sizeof(graph)/size
    cout<<sizeof(graph);

    // }
 
    return 0;
}
